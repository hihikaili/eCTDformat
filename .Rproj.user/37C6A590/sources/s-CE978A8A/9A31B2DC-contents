name: eCTD pdf
description: >
   Template for creating the landscape pages according to the eCTD Figure_Table Guidance
create_dir: FALSE

knit_to_folder <- function(input, ...) {
  rmarkdown::render(
    input,
    output_file = paste0(pathRmd, file, Ext()),
    envir = globalenv()
  )
}
